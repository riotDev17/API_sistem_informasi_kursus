// =========== GET AGAMA ===========
/**
 * @openapi
 * components:
 *   schemas:
 *     Get Agama Success:
 *       type: object
 *       properties:
 *         status:
 *           type: string
 *         message:
 *           type: string
 *         data:
 *           type: array
 *           items:
 *             type: object
 *             properties:
 *               id_agama:
 *                 type: string
 *               nama_agama:
 *                 type: string
 */

// =========== CREATE AGAMA ===========
/**
 * @openapi
 * components:
 *   schemas:
 *     Create Agama:
 *       type: object
 *       properties:
 *         nama_agama:
 *           type: string
 *     Create Agama Success:
 *       type: object
 *       properties:
 *         status:
 *           type: string
 *         message:
 *           type: string
 *         data:
 *           type: object
 *           properties:
 *             id_agama:
 *               type: string
 *             nama_agama:
 *               type: string
 */

// =========== GET AGAMA BY ID ===========
/**
 * @openapi
 * components:
 *   schemas:
 *     Get Agama By Id Success:
 *       type: object
 *       properties:
 *         status:
 *           type: string
 *         message:
 *           type: string
 *         data:
 *           type: object
 *           properties:
 *             id_agama:
 *               type: string
 *             nama_agama:
 *               type: string
 */

// =========== UPDATE AGAMA ===========
/**
 * @openapi
 * components:
 *   schemas:
 *     Update Agama:
 *       type: object
 *       properties:
 *         nama_agama:
 *           type: string
 *     Update Agama Success:
 *       type: object
 *       properties:
 *         status:
 *           type: string
 *         message:
 *           type: string
 *         data:
 *           type: object
 *           properties:
 *             id_agama:
 *               type: string
 *             nama_agama:
 *               type: string
 *
 */

// =========== DELETE AGAMA ===========
/**
 * @openapi
 * components:
 *   schemas:
 *     Delete Agama Success:
 *       type: object
 *       properties:
 *         status:
 *           type: string
 *         message:
 *           type: string
 *
 */
